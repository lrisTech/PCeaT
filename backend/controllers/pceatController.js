exports.processTime = function(req, res) {
  // Store data in variable
  var time = req.params.time;
  var boothNum = req.params.boothNum;
  // Add the seat and time to the database

  // Check if there are no errors
  // If no errors, return confirmation
};
